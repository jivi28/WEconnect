VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: base URL the event QR code points at, so a phone on the same
# network can reach /register (localhost is not reachable from a phone).
# Leave unset to use the current browser origin.
#   VITE_PUBLIC_BASE_URL=http://192.168.1.50:5173
# VITE_PUBLIC_BASE_URL=
